import TheOtherSide from "@/components/the-other-side";
import { type NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
    return (
            <TheOtherSide />
    );
};

export default Home;
